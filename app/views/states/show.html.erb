<% @results.each do |member| %>
	
	<p>
		<a href="/legislators/<%= member["bioguide_id"] %>">
		<%= member["title"] %>. <%= member["first_name"] %> <%= member["last_name"] %> - <%= member["party"] %>
		</a>
	</p>

<% end %>